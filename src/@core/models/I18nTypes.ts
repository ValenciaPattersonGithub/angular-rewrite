// Since i18n selections can be a runtime selection by the client, the various selectable
// values matching translation files will be lister here with their corresponding types
// i.e. 'fr_ca.json' = fr_ca. The names must match
export enum I18nTypes {
    en,
    es_mx,
    fr_ca
}
