import { EraReferenceIdentification } from './era-reference-identification';

describe('EraReferenceIdentification', () => {
  it('should create an instance', () => {
    expect(new EraReferenceIdentification()).toBeTruthy();
  });
});
