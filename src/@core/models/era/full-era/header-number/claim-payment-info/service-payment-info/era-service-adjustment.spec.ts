import { EraServiceAdjustment } from './era-service-adjustment';

describe('EraServiceAdjustment', () => {
  it('should create an instance', () => {
    expect(new EraServiceAdjustment()).toBeTruthy();
  });
});
