import { Component } from '@angular/core';

// When adding a new icon to the library, please also add the icon Id to the svg-icon.component.stories.ts list.

@Component({
    selector: 'app-svg-definitions',
    templateUrl: './svg-definitions.component.html',
    styleUrls: ['./svg-definitions.component.scss'],
})

export class SvgDefinitionsComponent { }
