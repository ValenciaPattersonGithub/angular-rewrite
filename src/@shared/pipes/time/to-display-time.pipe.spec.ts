import { ToDisplayTimePipe } from './to-display-time.pipe';

describe('ToDisplayTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ToDisplayTimePipe();
    expect(pipe).toBeTruthy();
  });
});
