import { RotTeethDisplayPipe } from './rot-teeth-display.pipe';

describe('RotTeethDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RotTeethDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
