import { AreaSelectorDisplayPipe } from './area-selector-display.pipe';

describe('AreaSelectorDisplayPipe', () => {
    it('create an instance', () => {
        const pipe = new AreaSelectorDisplayPipe();
        expect(pipe).toBeTruthy();
    });
});
