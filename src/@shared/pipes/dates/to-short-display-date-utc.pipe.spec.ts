import { ToShortDisplayDateUtcPipe } from './to-short-display-date-utc.pipe';

describe('ToShortDisplayDateUtcPipe', () => {
  it('create an instance', () => {
    const pipe = new ToShortDisplayDateUtcPipe();
    expect(pipe).toBeTruthy();
  });
});
