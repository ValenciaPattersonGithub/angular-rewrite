import { ToShortDisplayDatePipe } from './to-short-display-date.pipe';

describe('ToShortDisplayDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ToShortDisplayDatePipe();
    expect(pipe).toBeTruthy();
  });
});
