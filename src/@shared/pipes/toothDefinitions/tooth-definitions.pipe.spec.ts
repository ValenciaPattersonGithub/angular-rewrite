import { ToothDefinitionsPipe } from './tooth-definitions.pipe';

describe('ToothDefinitionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToothDefinitionsPipe();
    expect(pipe).toBeTruthy();
  });
});
