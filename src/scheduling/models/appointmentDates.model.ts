// This will go away after another level of refactoring. However this is the fastest resolution to what otherwise would be a lot more work.
export interface AppointmentDates {
    StartDate?: Date
    EndDate?: Date
}