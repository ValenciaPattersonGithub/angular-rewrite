'use strict';

angular.module('common.controllers').controller('InfoTileController', [
  '$scope',
  '$location',
  'localize',
  '$timeout',
  '$routeParams',
  '$route',
  'ListHelper',
  function (
    $scope,
    $location,
    localize,
    $timeout,
    $routeParams,
    $route,
    listHelper
  ) {
    // info tile controller placeholder
  },
]);
